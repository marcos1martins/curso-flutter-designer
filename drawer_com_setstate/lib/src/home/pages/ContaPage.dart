import 'package:flutter/material.dart';

class  ContaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Conta page", style: Theme.of(context).textTheme.body1,),
    );
  }
}